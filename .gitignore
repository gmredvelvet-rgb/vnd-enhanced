# El backend (Cloudflare Worker de licencias) ya NO vive aquí: se movió a su
# propio repo privado, gmredvelvet-rgb/velvet-license-backend. Este repo es
# público y el Worker sirve ya a 12 módulos, así que no pinta en el repo de uno
# solo de ellos. Ignorado para que una copia de trabajo local no vuelva a
# colarse en un commit.
backend/

# Local generated images
generated/

# DB migrations (server-side only)
sql/

# OS / editor
node_modules/
*.log
.DS_Store
Thumbs.db
*.zip
*.code-workspace
