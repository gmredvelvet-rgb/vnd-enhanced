# Backend dependencies only — source files ARE committed, node_modules are not
backend/node_modules/

# Local generated images
generated/

# DB migrations (server-side only)
sql/

# OS / editor
node_modules/
*.log
.DS_Store
Thumbs.db
*.zip
